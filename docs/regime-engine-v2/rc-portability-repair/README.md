# Regime Engine V2 — RC Portability Repair

**Reparación verificada: PASS.** Suite canónica 487/487, golden 464/464, histórico R2 1930/1930, ataques 110/110 y portabilidad 32/32. Build/tipos PASS; lint 0 errores y 0 warnings nuevos. `SR-A5-001` está resuelto en el delta local sin commit; shadow sigue bloqueado.

Esta entrega repara el blocker de verificación `SR-A5-001` sobre RC0 `f5fc7ecfd9e1f323d0ad3a43eccec1dbc500be1c` sin cambiar producto, metodología ni C03. RC0 conserva su identidad y su resultado histórico de portabilidad FAIL. La validación de esta reparación corresponde a **RC0 más el delta local inventariado**; no convierte retroactivamente RC0 en portable ni crea un nuevo freeze.

La entrada vigente es `docs/regime-engine-v2/release-candidate/run-verification.py --profile candidate`. Se mantienen las mismas listas de tests y los gates golden 464, histórico R2 1930, Sweeper 110, V1/V2, Designer, Groweer, tipos, lint y build. La [guía de reproducción](../release-candidate/verification-guide.md) contiene el comando completo.

Se eliminan las ramas ejecutables del perfil `worktree` y la reproducción de comandos serializados mediante `--resume-failed`. Los flags retirados fallan antes de crear outputs. No se importan tests ajenos para reproducir el antiguo conteo 592; el candidato conserva sus 487 casos canónicos, con golden/histórico contabilizados aparte.

Diez entrypoints del antiguo workspace devuelven ahora `exit 2` con `RETIRED_WORKSPACE_VERIFIER` e instrucciones de migración antes de leer evidencia o escribir sellos. Son ocho runners/selladores de Sweeper, Groweer, Maintainer y Designer, y los dos harnesses browser de Designer que dependían de un Playwright en un path local. Se eliminaron sus cuerpos ejecutables; sus originales y hashes están disponibles en RC0 Git y en `retired-entrypoints.json`. No se ocultó un fallo con skip/PASS ni se volvió a certificar una etapa histórica. Las capturas browser anteriores permanecen como evidencia histórica; esta reparación no afirma una nueva ejecución visual o de accesibilidad.

El comparador lint usa los diagnósticos y el hash ya versionados del parent y recupera sus fragmentos mediante Git. Las rutas absolutas de aquella ejecución se usan únicamente para interpretar rutas relativas, sin acceder al viejo checkout. Se verifica el tree del parent y se conserva la comparación de regla, mensaje, fragmento y multiplicidad. Ya no se requieren archivos locales derivados ni `--baseline-lint`/`--baseline-root`.

La preparación y la prueba se realizan en un checkout temporal limpio de RC0 con Git independiente y `npm ci` desde su lockfile. Solo se aplica el delta explícito de portabilidad. Los 177 archivos excluidos, los 16 documentos de Shadow Readiness y los demás archivos preexistentes del usuario se preservan. Los logs y la instrumentación de esta ejecución se guardan en `local/`, ignorado por Git; no son inputs del producto ni de futuras pruebas canónicas.

`test-portability.py` comprueba los rechazos de las rutas retiradas, la conservación de las listas canónicas y los casos de lint pertinentes. Las pruebas de portabilidad se contabilizan separadamente de los 487 casos del candidato. La primera ejecución de portabilidad (28/32) se conserva: falló la instrumentación ante una propiedad de Node 26 de solo lectura. Se corrigió únicamente ese harness y la ejecución final obtuvo 32/32; no se repitió innecesariamente la suite canónica. `verification.json` conserva los resultados efectivos y sus límites; `files-changed.json` inventaría el delta y sus hashes. `input-manifest.json` registra el estado de entrada completo. Los manifests de preparación y retirada describen sus checkpoints parciales; `verification.json` y `pristine-verification.json` contienen el cierre completo.

El alcance es la verificación del Release Candidate de Regime Engine V2, incluidos sus entrypoints históricos y sus previews. No se retiran herramientas ajenas de investigación offline por contener inputs externos explícitos. En particular, `scripts/build-td3-performance.mjs` es una herramienta TD3 preexistente ajena a este cierre y al runner canónico; permanece intacta. No se hace un claim de portabilidad universal de todos los pipelines de investigación del repositorio.

Los checks build pueden consultar fuentes V1 durante el prerenderizado. La instrumentación de red y sus límites se registran separadamente; esas respuestas no son una captura live V2. La reparación no avanza a calendario, adquisición V2, almacenamiento shadow ni readiness live.

El cierre de `SR-A5-001` se limita al delta verificado y queda sujeto a aceptación/freeze de Control Tower. Shadow permanece bloqueado y OFF; V1 mantiene autoridad pública. No se crea commit, no se hace push, deployment ni production cutover.
